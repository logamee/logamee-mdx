# logamee-mdx（简称 mdx）路线图

路线图只记录仓库中可验证的产品和工程状态。`[x]` 表示已交付并有代码/测试
证据，`[~]` 表示已有部分实现或实验性入口，`[ ]` 表示尚未承诺的计划。

## 当前基线

- [x] 五套以上本地主题皮肤，覆盖素笺、朱砂、天青、青花和竹青等纸面配色。
- [x] macOS、Linux、Windows 三平台 CI 冒烟/契约脚本与打包检查入口；这表示
  脚本已交付，不表示当前检出内容已包含 `.github/workflows/`。
- [x] Tauri 2 主窗口、文件关联、拖放、第二实例和独立预览窗口协议。
- [x] 工作区令牌、路径授权、快照/索引失效、持久写入和回收站结果分类。
- [x] Markdown 编辑、实时预览、资源预览、导出、外部变化和崩溃草稿恢复。

## 已完成增强

- [x] 统一 `MutationOutcome` 与 `SnapshotReceipt`，禁止把不确定文件操作显示为成功。
- [x] PDF/DOCX/资源/导出大小上限和前端协议精确键解码。
- [x] 中文/英文、本地化模态反馈、主题合约测试和响应式工作区布局。
- [x] 10k/100k 工作区索引性能基线与非破坏性 M3 门禁评估脚本。

## 进行中

- [~] 跨平台桌面生命周期、文件关联和视觉验收的真实机器证据收集。
- [~] Excalidraw 资源配对、HTML 嵌入和多窗口媒体插入在更多真实文档中的回归。
- [~] 将架构、安全、生命周期和验证文档持续同步到实现与测试。

## 计划路线

- [ ] 支持在 Markdown 正文中嵌入并播放工作区内的视频和音频。当前音视频
  文件仅支持独立只读预览，音频拖入 Markdown 只生成普通链接，不能视为
  正文内嵌能力；交付前需明确兼容语法、授权资源读取、格式与失败回退、
  无障碍、导出降级和跨平台验收。
- [ ] 为导出配置建立版本化、可迁移且有向后兼容策略的结构规范。
- [ ] 评估可选同步/协作能力；在安全、权限和数据模型决策完成前不实现。
- [ ] 补齐当前缺失的 `.github/workflows/platform-ci.yml` 和 `release.yml`，再完成
  目标平台人工验收和可审计产物证据；补齐前发布工作流契约测试不可用，不能
  宣称发布门禁完整通过。
