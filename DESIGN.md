# 设计规范

本规范适用于 logamee-mdx（简称 mdx）。

## 事实来源

- 状态：生效
- 最近更新：2026-08-14
- 主要产品界面：本地工作区、Markdown 编辑器、实时预览、文档大纲、文件树、资源预览、导出、设置和可选的独立预览窗口。
- 已审阅证据：`package.json`、`src-tauri/tauri.conf.json`、`src/App.tsx`、`src/components/AppToolbar.tsx`、`src/components/FileSidebar.tsx`、`src/components/EditorPane.tsx`、`src/components/PreviewPane.tsx`、`src/components/FeedbackDialog.tsx`、`src/lib/theme.ts`、`src/styles/base.css`、`src/styles/app-shell.css`、`src/styles/markdown-preview.css`、`src/styles/responsive.css`、`src/types.ts`，以及 `src-tauri/src/` 下的 Rust 模块。
- 已观察事实：mdx 是本地优先的 Tauri 2 桌面应用。它可以编辑 Markdown、HTML 和 Excalidraw 文本文档，预览图片、音频、视频、PDF 和 DOCX，并把工作区、文件、窗口和恢复状态保留在本地应用边界内。
- 工作假设：当前产品是一款面向单用户的专注型桌面编辑器。除非未来产品决策另有记录，否则云协作、账号身份、服务端持久化、遥测和浏览器优先的信息架构不属于当前版本。

## 品牌

- 品牌性格：安静、可靠、清晰、克制，像一张适合长时间书写的纸面工作台；工具感来自秩序和反馈，而不是装饰。
- 可信信号：当前文件名和保存状态始终可见，工作区边界明确，外部修改和冲突有明确分支，危险操作有可逆或可恢复路径，预览不掩盖原始文档。
- 禁止倾向：通用 SaaS 仪表盘、紫蓝渐变主导、霓虹光晕、光球或背景虚化光斑、无限滚动装饰、卡片套卡片、过大的营销首屏、仅靠颜色表达状态、把不确定写入显示成成功，以及与文档无关的插画。

## 产品目标

- 目标：让用户快速打开本地 Markdown 工作区，连续编辑并检查渲染结果；让图片、媒体、HTML、Excalidraw、PDF 和 DOCX 等相邻资料在同一工作流内可检查；在保存、外部变化、崩溃恢复和工作区变更时保持数据可信。
- 非目标：云端同步或协作、在线内容管理系统、账号或社交能力、完整集成开发环境、任意路径的文件管理器、把所有二进制格式转成可编辑文本，以及未经授权的后台常驻服务。
- 成功信号：首次打开即可理解“工作区/编辑器/预览”的关系；编辑与预览不互相遮挡；保存状态和冲突分支可判断；工作区操作失败不会静默丢失；窄窗口和深色模式仍可读；新增行为有可重复测试证据。

## 用户与任务

- 主要用户：使用本地 Markdown 管理知识的开发者和研究者、维护课程或技术文档的作者、需要离线阅读资料的学生，以及审阅导出结果的协作者。
- 用户任务：选择工作区、快速打开或搜索文档、编辑 Markdown、跳转大纲、查看实时预览、插入或检查资源、处理外部修改与未保存退出、导出离线 HTML/长图 PNG/Excalidraw 三件套、恢复崩溃草稿。
- 主要使用场景：长时间桌面书写、键盘驱动的快速切换、窄屏或分屏窗口、离线环境、工作区被其他程序同时修改，以及从文件管理器拖放或通过文件关联启动。

## 信息架构

- 主导航：顶部工具栏承载品牌、导出、快速打开、工作区搜索、当前文档、保存状态和设置；主工作区承载文件树/大纲、编辑器和预览。
- 核心界面：单一主窗口；按文件类型切换 Markdown/HTML/Excalidraw 编辑或图片/媒体/PDF/DOCX 预览；设置、快速打开、工作区搜索、导出、外部变更、保存冲突、未保存退出和崩溃恢复均使用对话框；预览和 HTML 嵌入可以在独立窗口呈现。
- 内容层级：当前文档状态 -> 工作区和文件树 -> 编辑表面 -> 预览表面 -> 辅助对话框。文件树与大纲是同一侧栏的两个明确视图，不在编辑内容中插入第二套导航。

## 设计原则

- 文档优先：文档内容和可验证状态优先于工具栏装饰；每个控件都应服务于打开、编辑、检查、保存或导出。
- 状态可信：`saved`、`edited`、`working`、外部变化、冲突、恢复和不确定结果必须有文字与可执行动作，不能只靠颜色或动画。
- 层级唯一：顶部工具栏、侧栏、编辑器、预览各有稳定职责；避免把同一动作复制到多个争夺注意力的浮层。
- 渐进复杂度：常用操作在首屏，格式和资源细节按需加载；PDF、DOCX、Excalidraw 等重模块保持懒加载。
- 本地归属：用户选择的工作区是数据边界；任何跨目录、跨授权或隐式网络行为都需要显式产品决策。
- 取舍：允许编辑器和预览在宽屏并列、窄屏上下排列；优先稳定尺寸和可恢复性，接受较少的装饰与即时动画。

## 视觉语言

- 颜色：使用 `src/styles/themeTokens.ts` 定义的标准令牌，例如 `--bg`、`--toolbar`、`--sidebar`、`--text-main`、`--accent`、`--theme-emphasis`、`--focus-ring`、`--danger`、`--warning`、`--success` 及编辑器/预览令牌。现有 9 套主题皮肤为素笺·青黛、朱批·丹砂、汝瓷·天青、青花·苏青、宋版·竹青、杏笺·赭石、春笺·豆青、烟岚·缃素和玄卷·松烟；外观设置必须把 9 套色板明确呈现为可选项，不依赖原生下拉菜单的隐藏滚动区域。状态色必须保留业务语义，不得被主题色吞没。
- 字体：使用系统无衬线字体保证跨平台界面一致性，预览沿用 `public/styles/typora-theme/typora-jinxiu.css` 的纸面排版；正文、按钮和状态文字保持可读字号，代码和版本/计数信息可使用等宽数字。禁止随视口宽度直接缩放字号。
- 间距与布局节奏：顶部工具栏固定 48px；桌面工作区以侧栏 + 主内容分栏，编辑器与预览之间使用稳定的拖拽分隔；使用 10-24px 控件间距和 16-56px 阅读内边距，首屏不得被弹层或工具栏挤压。
- 形状、圆角与层级：工具按钮和输入控件使用约 6px 小圆角，代码/内容表面最多 8px；用细边框、层次化背景和一层克制阴影表达分区。不得把页面做成嵌套卡片墙。
- 动效：交互反馈使用短暂的颜色、边框或轻微位移过渡；加载只在确有工作时显示；禁止持续扫描、频闪和与文档无关的循环动画，并尊重 `prefers-reduced-motion`。
- 图像与图标：图标使用 `lucide-react`，图标按钮必须有 `aria-label` 和 `title`；媒体和文档预览显示真实内容或稳定的空状态，不使用装饰性首屏 SVG 或无关素材图。

## 组件

- 优先复用的现有组件：`AppToolbar`、`FileSidebar`、`FileTreeRows`、`EditorPane`、`PreviewPane`、`PaneHeader`、`PaneResizer`、`PopoutPaneShell`、`FeedbackDialog`、`UnsavedExitDialog`、`DocumentSaveConflictDialog`、`ExternalFileChangeDialog`、`CrashDraftRecoveryDialog`、`SettingsDialog`、`ExportDialog`、`WorkspaceSearchDialog`、`WorkspaceEntryDialog`、`WorkspaceMoveDialog`，以及 `src/components/` 下的预览组件。
- 新增或修改组件：优先扩展现有面板、对话框和工作区组件；只有当一个交互跨越多个窗口或文件类型且已有稳定协议时，才抽出共享组件。新组件必须在所属层拥有状态，不得把 Tauri 调用散落到展示层。
- 变体与状态：Markdown/HTML/Excalidraw 可编辑；`image`、`video`、`audio`、PDF、DOCX 只读预览；支持明暗模式、主题皮肤、中英文、侧栏折叠、编辑器/预览独立窗口，以及 `saved`、`edited`、`working`、加载、空、错误、禁用、确认和不确定变更等状态。
- 令牌与组件归属：主题令牌归 `src/styles/base.css`、`src/styles/themeTokens.ts` 和 `src/lib/theme.ts`；工作区布局归 `src/styles/app-shell.css`、`responsive.css` 与布局钩子；协议解码归 `src/lib/`；文件系统和授权归 `src-tauri/src/`。不要新建平行令牌层或第二套 IPC 适配器。

## 无障碍

- 目标标准：以 WCAG 2.1 AA 为目标，覆盖对比度、键盘访问、焦点可见性、语义、缩放和减少动态效果。
- 键盘与焦点：文件树和大纲保留 `tree`/`treeitem` 键盘导航；菜单和对话框使用正确的 `menu`、`dialog`、`tab`/`tabpanel` 语义；弹窗关闭后恢复触发焦点；编辑器快捷键不得阻断文本输入和系统辅助功能。
- 对比度与可读性：正文和辅助文字使用令牌中明确的对比度，不使用低透明度品牌文字；深色主题的边框仍需可辨；状态同时提供文字、图标或可读标签。
- 屏幕阅读器语义：图标按钮有名称，当前文档和保存状态使用可感知的 `aria-label`/`aria-live`，树、对话框、预览地标和标题层级保持稳定；不把纯视觉分隔线当作信息。
- 减少动态与感官考虑：`prefers-reduced-motion` 下关闭或压平过渡、旋转和弹出动画；错误、冲突和恢复信息不依赖颜色或声音。

## 响应式行为

- 支持的断点与设备：Tauri 默认窗口最小约 900x600；CSS 在 980px 和 640px 附近切换布局，同时支持浏览器开发预览的窄视口。
- 布局适配：宽屏为侧栏 + 编辑器/预览并列；中等宽度隐藏编辑器/预览拖拽条并改为上下两行；窄屏压缩工具栏、隐藏次要文字、保留可折叠侧栏和可滚动的预览工具栏；对话框使用视口内最大高度，不产生页面溢出。
- 触控与悬停差异：关键目标保持至少约 40px 的可触达区域；悬停只增强边框或背景，不是完成操作的唯一方式；拖放提供键盘或菜单替代路径。

## 交互状态

- 加载：懒加载预览使用 `LazyPreviewBoundary`，保持面板尺寸并给出本地化加载状态；索引、读取和导出期间显示工作中状态，不冻结无关面板。
- 空状态：未选择工作区、空目录、无大纲、空媒体或无可恢复草稿时，说明当前状态和下一步有效动作，不显示伪造内容。
- 错误：文件、权限、解析、预览、通信和保存错误统一转为友好的模态对话框；技术细节留在日志或测试证据中，不直接泄漏给用户。
- 成功：保存、导出、复制或恢复成功后更新稳定的状态文字或短暂对话反馈；不要用常驻通知占用编辑空间。
- 禁用：不适用的文件类型或尚未准备好的操作使用禁用状态并说明原因；禁用不得掩盖真正的权限或冲突问题。
- 离线或网络缓慢：核心编辑和本地预览不依赖网络；字体、媒体或外部 HTML 失败时保留稳定占位和可读错误，不清空已有文档。

## 文案语气

- 语气：简洁、直接、诚实，以动作和状态为中心；中文默认自然，英文与中文表达同一事实。
- 术语：使用“工作区”“当前文档”“实时预览”“外部修改”“保存冲突”“崩溃草稿”“不确定结果”等稳定术语；文件类型名称沿用 Markdown、HTML、Excalidraw、PDF、DOCX。
- 微文案规则：说明发生了什么、当前数据是否已提交、用户能做什么；避免“智能”“无缝”“一键解决”等夸张承诺，也不要把教程段落塞进界面。

## 实现约束

- 框架与样式系统：React 19、TypeScript 6、Vite 8、Tauri 2、CodeMirror、React Markdown/GFM、KaTeX、Mermaid、Excalidraw、PDF.js、Mammoth、项目 CSS 变量和 `lucide-react`。
- 设计令牌约束：只使用标准主题令牌；任何新颜色、间距或状态先确认能否由现有令牌表达，并同步 `themeTokens` 契约测试；不新增依赖来替代已有能力。
- 性能约束：保留 PDF、DOCX、Excalidraw 等重模块的懒加载；遵守 PDF 64 MiB、DOCX 32 MiB 的输入上限；工作区索引和媒体资源使用既有有界协议，避免每次渲染重复读取或产生 N+1 IPC。
- 兼容性约束：保留 Markdown 文件关联（`.md`、`.mdx`、`.markdown`、`.mdown`、`.mkd`）、中文/英文、明暗模式和现有主题皮肤、主窗口/独立窗口通信、CSP/资源协议、跨平台路径与原子写入边界。旧 `mmd` 事件、存储键、CSS 类、格式标记、环境变量和应用标识符属于兼容面，不随显示名称直接迁移。
- 测试与截图要求：界面、协议或安全改动先运行聚焦 Vitest 或 Rust 测试，再按 `docs/testing/validation-matrix.md` 运行 `npm test`、`npm run typecheck`、`npm run lint`、`npm run build` 和 `cargo test --manifest-path src-tauri/Cargo.toml`；具备浏览器或桌面截图能力时检查宽屏、窄屏、深色和主要文件类型。

## 待决问题

- [ ] 产品计划：支持在 Markdown 实时预览中嵌入并播放本地音频；视频已支持
  图片样式语法和工作区授权资源播放。视频常见容器按原生
  `<video>` 或 `mpegts.js` 路径处理，授权资源通过带令牌的回环 HTTP 预览服务提供，
  以兼容不同 Tauri WebView；编码兼容仍取决于各平台 WebView；音频正文语法、字幕/文本替代
  和导出回退仍待后续明确。
- [ ] 产品：是否长期保持纯本地单用户模型，还是需要明确同步/协作路线；影响数据模型、权限和安全边界。
- [ ] 产品：导出配置目前只保留通用结构，未来是否需要稳定且用户可编辑的结构规范；影响设置兼容与迁移策略。
- [ ] 工程：浏览器开发模式是否需要成为正式的无 Tauri 降级产品；影响资源 URL、文件访问和错误提示。
- [ ] 产品：Excalidraw、HTML 嵌入和二进制预览的跨平台能力是否需要统一到一个可导出的验收矩阵；影响发布门禁和测试夹具。
- [ ] 工程：是否应在窗口或应用销毁时对独立 HTML 预览站点显式调用 `stop_all_sites()`；当前仅有有界站点池及后续替换/授权失败回收，不能把立即释放当作既有保证。
